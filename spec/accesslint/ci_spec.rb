require 'spec_helper'

describe Accesslint::Ci do
  it 'has a version number' do
    expect(Accesslint::Ci::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
